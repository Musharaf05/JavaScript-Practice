let x

const numbers=[12, 30, 44, 21, 16, 10]
const fruits=["Apple","Orange","Watermelon"]
const mixed=[10,"hello",13]

x=numbers.length // 6 (Gives the number of elements in an array)

fruits[1]="Papaya" // changing array value

x=fruits

console.log(x)